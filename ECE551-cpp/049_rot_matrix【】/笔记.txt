整个过程中比较困难的debug出现在处理输入文件字符个数长度宽度超限的情况。

对于长度太短的情况，由于每停在行末尾的文件指针一定少于预计的长度，所以较好判断；
对于长度太长的情况，则需要给预定的数组增加一些值，从而不会产生stackoverflow、*** stack smashing detected ***: ./rotateMatrix terminated的情况。然而，这种方法会增加一些数组所占内存的大小，也不是十分优雅。

最好的方法应该是用指针来解决，但指针我确实不太会用，只能等后面review的时候再看。
